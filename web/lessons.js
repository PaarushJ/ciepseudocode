/* Lesson content for the Learn panel.

   Ported from the reference Cambridge Pseudocode course: 26 sequential
   lessons written in Markdown and rendered by web/markdown.js.

   Each lesson is { title, content } with an optional `solution` used by the
   'Reveal solution' block on challenge lessons. Content may use headings,
   tables, block quotes, fenced code, ordered/unordered lists and task-list
   checkboxes (which the Learn panel makes interactive and remembers). */

export const LESSONS = [
{
  title: "Welcome",
  content: "# Welcome to Cambridge Pseudocode\n\n**Pseudocode** is a way of writing out the logic of a program in plain, structured language. It isn't a real programming language that a computer can run directly — instead it's a tool for thinking through and communicating ideas clearly, following a consistent set of rules.\n\nCambridge Pseudocode is the specific style used in **Cambridge IGCSE and A-Level Computer Science** exams. You'll be expected to read it, trace through it, and write it yourself.\n\n---\n\n## What You'll Learn\n\nThis guide takes you from absolute basics all the way through to the topics that appear in exams. By the end you'll be able to:\n\n- **Print** output and work with variables\n- Use **conditions** — IF statements and CASE\n- Repeat actions with **loops** — FOR, WHILE, and REPEAT\n- Write reusable code with **procedures** and **functions**\n- Store collections of data in **arrays**\n- Work with **strings** — measuring, slicing, and transforming text\n- Save and load data using **file handling**\n- Implement classic algorithms — **linear search** and **bubble sort**\n\n---\n\n## How to Use This Guide\n\nEach lesson introduces one concept with examples you can run immediately in the editor on the right. Try every example yourself — reading code is not the same as writing it.\n\nSome lessons end with a **challenge**. Have a go before revealing the solution.\n\n---\n\n> **No experience needed.** If you've never written a line of code before, start at lesson 1 and work through in order. Everything builds on what came before.\n\nLet's get started."
},
{
  title: "Printing Text",
  content: "# Printing Text with OUTPUT\n\nThe very first thing most programmers learn is how to print something to the screen. In Cambridge Pseudocode, you use the `OUTPUT` keyword.\n\n## Syntax\n\n```\nOUTPUT \"Hello, World!\"\n```\n\nThe text you want to print goes inside **double quotes**. This is called a **string** — it's just a piece of text.\n\n## Example\n\n```\nOUTPUT \"Hello!\"\nOUTPUT \"My name is Alice.\"\nOUTPUT \"I am learning pseudocode.\"\n```\n\nThis would print:\n\n```\nHello!\nMy name is Alice.\nI am learning pseudocode.\n```\n\n> **Remember:** Any text you want to print must be wrapped in double quotes `\"like this\"`."
},
{
  title: "Printing Numbers",
  content: "# Printing Numbers\n\nYou can also print numbers with `OUTPUT`. Numbers do **not** need quotes around them.\n\n## Example\n\n```\nOUTPUT 42\nOUTPUT 100\nOUTPUT 3.14\n```\n\nThis would print:\n\n```\n42\n100\n3.14\n```\n\n## Quotes vs No Quotes\n\n| What you want to print | How to write it |\n|---|---|\n| A piece of text | `OUTPUT \"Hello\"` |\n| A number | `OUTPUT 42` |\n\n> **Tip:** If you put quotes around a number — like `OUTPUT \"42\"` — it will still print, but the computer treats it as text, not a number. This matters when you do maths later."
},
{
  title: "What is a Variable?",
  content: "# What is a Variable?\n\nA **variable** is like a labelled box. You give the box a name, and you can store a value inside it. Later, you can look inside the box to use that value.\n\n## A Simple Analogy\n\nImagine you have a box with the label **Age** written on it. You put the number **16** inside. Whenever you need to know someone's age, you look in the **Age** box.\n\n```\n┌─────────┐\n│   Age   │  ← the name\n│   16    │  ← the value stored inside\n└─────────┘\n```\n\nIn pseudocode, variables work exactly like this.\n\n---\n\nBefore you can use a variable, you need to do two things:\n\n1. **Declare** it — tell the computer the name and what type of value it will hold\n2. **Assign** it — put a value inside it\n\nWe'll look at both of these on the next steps."
},
{
  title: "Declaring a Variable",
  content: "# Declaring a Variable with DECLARE\n\nBefore you can use a variable, you must **declare** it. This tells the computer:\n- The **name** of the variable\n- The **type** of value it will hold\n\n## Syntax\n\n```\nDECLARE VariableName : DATATYPE\n```\n\n## Data Types\n\n| Type | What it stores | Example values |\n|---|---|---|\n| `INTEGER` | Whole numbers | `5`, `-3`, `100` |\n| `REAL` | Decimal numbers | `3.14`, `-0.5` |\n| `STRING` | Text | `\"Alice\"`, `\"hello\"` |\n| `CHAR` | A single character | `'A'`, `'z'`, `'3'` |\n| `BOOLEAN` | True or false | `TRUE`, `FALSE` |\n\n## Examples\n\n```\nDECLARE Age : INTEGER\nDECLARE Name : STRING\nDECLARE Price : REAL\nDECLARE IsStudent : BOOLEAN\n```\n\n> **Note:** Declaring a variable just creates the box and labels it. It doesn't put anything inside yet — that's what assignment is for.\n\n---\n\n## Constants\n\nSome values never change while the program runs — a VAT rate, the size of an\narray, the maximum score. Declare those as a **constant** with `CONSTANT`, and\ngive the value straight away:\n\n```\nCONSTANT VAT = 0.2\nCONSTANT MaxScore = 100\nCONSTANT Greeting = \"Hello\"\n```\n\nA constant has no `: DATATYPE` — its type comes from the value you give it. Once\nset it cannot be assigned to again, which is exactly the point: it protects the\nvalue from being changed by accident.\n\n```\nCONSTANT MaxSize = 10\nDECLARE Scores : ARRAY[1:MaxSize] OF INTEGER\n\nScores[3] <- 7\nOUTPUT Scores[3]\n```\n\n> **Why bother?** If the maximum ever changes you edit one line, not twenty. And\n> a reader can see at a glance that `MaxSize` is fixed."
},
{
  title: "Assigning a Value",
  content: "# Assigning a Value with <-\n\nOnce a variable is declared, you can put a value into it using the **assignment arrow** `<-`.\n\n## Syntax\n\n```\nVariableName <- Value\n```\n\nThink of `<-` as meaning **\"gets the value of\"**. The arrow points from the value to the variable.\n\n## Examples\n\n```\nDECLARE Age : INTEGER\nAge <- 16\n\nDECLARE Name : STRING\nName <- \"Alice\"\n\nDECLARE Price : REAL\nPrice <- 9.99\n```\n\n## What Happens Step by Step\n\n```\nDECLARE Score : INTEGER    ← creates an empty box called Score\nScore <- 100               ← puts 100 into the Score box\n```\n\n> **Important:** The variable name always goes on the **left** of `<-`, and the value always goes on the **right**."
},
{
  title: "Using Variables with OUTPUT",
  content: "# Using Variables with OUTPUT\n\nOnce a variable has a value, you can print it using `OUTPUT`.\n\n## Example\n\n```\nDECLARE Name : STRING\nName <- \"Alice\"\n\nOUTPUT Name\n```\n\nOutput:\n```\nAlice\n```\n\nNotice there are **no quotes** around `Name` in the OUTPUT line. That's because you want to print the *value stored inside* the variable, not the word \"Name\".\n\n---\n\n## Printing Text and Variables Together\n\nYou can combine text and variables in one OUTPUT statement using the `&` symbol:\n\n```\nDECLARE Name : STRING\nName <- \"Alice\"\n\nOUTPUT \"Hello, \" & Name & \"!\"\n```\n\nOutput:\n```\nHello, Alice!\n```\n\nYou can also use a **comma** `,` instead of `&` to separate items in an OUTPUT statement:\n\n```\nOUTPUT \"Hello, \", Name, \"!\"\n```\n\nBoth produce the same output. However, commas are **only allowed inside OUTPUT statements** — you cannot use them to join strings anywhere else in your code (for example, when assigning to a variable). Stick to `&` everywhere else.\n\n## Another Example\n\n```\nDECLARE Age : INTEGER\nAge <- 16\n\nOUTPUT \"You are \" & Age & \" years old.\"\n```\n\nOutput:\n```\nYou are 16 years old.\n```"
},
{
  title: "Arithmetic Operators",
  content: "# Arithmetic Operators\n\nYou can do maths directly in your pseudocode. These are the operators available:\n\n| Operator | Meaning | Example | Result |\n|---|---|---|---|\n| `+` | Addition | `10 + 3` | `13` |\n| `-` | Subtraction | `10 - 3` | `7` |\n| `*` | Multiplication | `10 * 3` | `30` |\n| `/` | Division | `10 / 4` | `2.5` |\n| `MOD` | Remainder after division | `10 MOD 3` | `1` |\n| `DIV` | Integer division (whole number only) | `10 DIV 3` | `3` |\n| `^` | Exponentiation (power) | `2 ^ 8` | `256` |\n\n---\n\n## Using Operators\n\nYou can use these in assignments, OUTPUT statements, or anywhere a value is expected:\n\n```\nDECLARE x : INTEGER\nDECLARE y : INTEGER\nx <- 10\ny <- 3\n\nOUTPUT x + y\nOUTPUT x - y\nOUTPUT x * y\nOUTPUT x / y\n```\n\nOutput:\n```\n13\n7\n30\n3.3333...\n```\n\n---\n\n## MOD and DIV\n\nThese two are easy to confuse, so here's the difference:\n\n- `DIV` gives you the **whole number** part of a division — it throws away the remainder\n- `MOD` gives you **only the remainder**\n\n```\nOUTPUT 10 DIV 3\nOUTPUT 10 MOD 3\n```\n\nOutput:\n```\n3\n1\n```\n\nBecause 10 ÷ 3 = **3** remainder **1**.\n\n`MOD` is useful for things like checking if a number is even or odd:\n\n```\nDECLARE Num : INTEGER\nNum <- 7\n\nIF Num MOD 2 = 0 THEN\n    OUTPUT \"Even\"\nELSE\n    OUTPUT \"Odd\"\nENDIF\n```\n\nOutput:\n```\nOdd\n```\n\n---\n\n## Exponentiation\n\n`^` raises a number to a power:\n\n```\nOUTPUT 2 ^ 8\nOUTPUT 3 ^ 2\n```\n\nOutput:\n```\n256\n9\n```\n\n---\n\n## Order of Operations\n\nPseudocode follows standard mathematical order — multiplication and division happen before addition and subtraction. Use brackets to control the order:\n\n```\nOUTPUT 2 + 3 * 4\nOUTPUT (2 + 3) * 4\n```\n\nOutput:\n```\n14\n20\n```"
},
{
  title: "Getting User Input",
  content: "# Getting User Input with INPUT\n\nSo far you've been setting variable values yourself in your code. But programs often need to ask the **user** to type something in.\n\nYou do this with the `INPUT` keyword.\n\n## Syntax\n\n```\nINPUT VariableName\n```\n\nThis pauses the program, waits for the user to type something and press Enter, then stores what they typed into the variable.\n\n## Example\n\n```\nDECLARE Name : STRING\nINPUT Name\nOUTPUT \"Hello, \" & Name & \"!\"\n```\n\nIf the user types **Alice**, the output will be:\n```\nHello, Alice!\n```\n\n---\n\n## A Note on INPUT\n\nYou should always **declare** your variable before using `INPUT`:\n\n```\nDECLARE Age : INTEGER   ← declare first\nINPUT Age               ← then get input\nOUTPUT \"You are \" & Age & \" years old.\"\n```\n\n> **Tip:** It's good practice to prompt the user before an INPUT so they know what to type. You can do this with an OUTPUT on the line before:\n> ```\n> OUTPUT \"Enter your name:\"\n> INPUT Name\n> ```"
},
{
  title: "Your First Program",
  content: "# Your Challenge\n\nYou've learned everything you need. Now write a complete program yourself using the IDE on the right.\n\n## What your program should do\n\n1. Ask the user for their **name** and store it\n2. Ask the user for their **age** and store it\n3. Print a greeting that includes their name\n4. Print a sentence that includes their age\n\n## Expected output\n\nIf the user enters **Alice** and **16**, your program should print:\n\n```\nWhat is your name?\nHow old are you?\nHello, Alice!\nYou are 16 years old.\n```\n\n---\n\n## Checklist\n\nBefore revealing the solution, make sure your program:\n\n- [ ] Has **declared** both variables with the correct data types\n- [ ] Uses **OUTPUT** to prompt the user before each **INPUT**\n- [ ] Uses `&` to join text and variables in the output lines\n- [ ] Produces the correct output when you run it\n\n> **Tip:** Type your inputs in the terminal's input box before pressing Run — put each value on its own line.",
  solution: "DECLARE Name : STRING\nDECLARE Age : INTEGER\n\nOUTPUT \"What is your name?\"\nINPUT Name\n\nOUTPUT \"How old are you?\"\nINPUT Age\n\nOUTPUT \"Hello, \" & Name & \"!\"\nOUTPUT \"You are \" & Age & \" years old.\""
},
{
  title: "IF Statements",
  content: "# IF Statements\n\nSometimes you want your program to make a decision — do one thing if something is true, and something else if it isn't. That's what an IF statement does.\n\n## Basic Syntax\n\n```\nIF condition THEN\n    // code that runs if condition is true\nENDIF\n```\n\n## With an ELSE\n\n```\nIF condition THEN\n    // runs if condition is true\nELSE\n    // runs if condition is false\nENDIF\n```\n\nThe `ELSE` part is **optional** — you only need it if you want something to happen when the condition is false.\n\n---\n\n## A Simple Example\n\n```\nDECLARE Age : INTEGER\nAge <- 17\n\nIF Age >= 18 THEN\n    OUTPUT \"You are an adult.\"\nELSE\n    OUTPUT \"You are not yet an adult.\"\nENDIF\n```\n\nOutput:\n```\nYou are not yet an adult.\n```\n\nBecause `Age` is 17, the condition `Age >= 18` is false, so the ELSE branch runs.\n\n---\n\n## Comparison Operators\n\nThese are the operators you can use in conditions:\n\n| Operator | Meaning |\n|---|---|\n| `=` | Equal to |\n| `<>` | Not equal to |\n| `<` | Less than |\n| `>` | Greater than |\n| `<=` | Less than or equal to |\n| `>=` | Greater than or equal to |\n\n---\n\n## IF Without ELSE\n\nIf you only want to do something in one case, you can leave the ELSE out entirely:\n\n```\nDECLARE Score : INTEGER\nScore <- 95\n\nIF Score > 90 THEN\n    OUTPUT \"Excellent!\"\nENDIF\n```\n\nOutput:\n```\nExcellent!\n```\n\nIf `Score` were 80, nothing would be printed at all.\n\n---\n\n## Combining Conditions with AND / OR / NOT\n\nYou can check more than one condition at once using `AND` and `OR`.\n\n### AND — both must be true\n\n```\nDECLARE Age : INTEGER\nDECLARE HasID : BOOLEAN\nAge <- 20\nHasID <- TRUE\n\nIF Age >= 18 AND HasID = TRUE THEN\n    OUTPUT \"Entry allowed.\"\nELSE\n    OUTPUT \"Entry denied.\"\nENDIF\n```\n\nOutput:\n```\nEntry allowed.\n```\n\nWith `AND`, **every** condition must be true for the IF body to run. If either one is false, it goes to ELSE.\n\n### OR — at least one must be true\n\n```\nDECLARE IsStudent : BOOLEAN\nDECLARE IsTeacher : BOOLEAN\nIsStudent <- FALSE\nIsTeacher <- TRUE\n\nIF IsStudent = TRUE OR IsTeacher = TRUE THEN\n    OUTPUT \"Access granted.\"\nELSE\n    OUTPUT \"Access denied.\"\nENDIF\n```\n\nOutput:\n```\nAccess granted.\n```\n\nWith `OR`, the IF body runs if **any one** of the conditions is true.\n\n### NOT — flip the result\n\n`NOT` reverses a condition. If something is true, `NOT` makes it false, and vice versa.\n\n```\nDECLARE IsLoggedIn : BOOLEAN\nIsLoggedIn <- FALSE\n\nIF NOT IsLoggedIn THEN\n    OUTPUT \"Please log in.\"\nENDIF\n```\n\nOutput:\n```\nPlease log in.\n```\n\nBecause `IsLoggedIn` is `FALSE`, `NOT IsLoggedIn` is `TRUE`, so the IF body runs.\n\n### Quick reference\n\n| Operator | Runs if… |\n|---|---|\n| `AND` | All conditions are true |\n| `OR` | At least one condition is true |\n| `NOT` | The condition is false |\n\n---\n\n## Important Rules\n\n- Always end with `ENDIF` — the program needs to know where the IF statement finishes\n- Indent the code inside the IF and ELSE blocks to keep it readable\n- The condition does not need brackets around it"
},
{
  title: "Nested Selection",
  content: "# Nested Selection\n\nA nested IF is simply an IF statement inside another IF statement. You use it when a decision depends on a previous decision.\n\n## Syntax\n\n```\nIF condition1 THEN\n    IF condition2 THEN\n        // runs if both condition1 AND condition2 are true\n    ELSE\n        // runs if condition1 is true but condition2 is false\n    ENDIF\nELSE\n    // runs if condition1 is false\nENDIF\n```\n\nEach inner IF must have its own `ENDIF`. Indentation is key — it makes it clear which ENDIF belongs to which IF.\n\n---\n\n## Example: Ticket Pricing\n\n```\nDECLARE Age : INTEGER\nDECLARE IsStudent : BOOLEAN\nAge <- 20\nIsStudent <- TRUE\n\nIF Age < 18 THEN\n    OUTPUT \"Child ticket: £5\"\nELSE\n    IF IsStudent = TRUE THEN\n        OUTPUT \"Student ticket: £8\"\n    ELSE\n        OUTPUT \"Adult ticket: £12\"\n    ENDIF\nENDIF\n```\n\nOutput:\n```\nStudent ticket: £8\n```\n\nThe program first checks age. Since `Age` is 20 (not under 18), it goes to the ELSE. There it checks if the person is a student — they are, so it prints the student price.\n\n---\n\n## How to Read It\n\nWork through nested IFs from the outside in:\n\n1. Is the outer condition true?\n   - **Yes** → run the outer IF body (which may contain more IFs)\n   - **No** → jump to the outer ELSE\n\n2. Inside the body, check the next condition the same way.\n\n---\n\n## Chaining Conditions\n\nWhen you have several options, nest a new `IF` inside each `ELSE`:\n\n```\nDECLARE Score : INTEGER\nScore <- 72\n\nIF Score >= 90 THEN\n    OUTPUT \"Grade: A\"\nELSE\n    IF Score >= 70 THEN\n        OUTPUT \"Grade: B\"\n    ELSE\n        IF Score >= 50 THEN\n            OUTPUT \"Grade: C\"\n        ELSE\n            OUTPUT \"Grade: F\"\n        ENDIF\n    ENDIF\nENDIF\n```\n\nOutput:\n```\nGrade: B\n```\n\nThe program checks each condition in order and stops as soon as one is true. Only one branch ever runs. Each `ELSE` contains a brand new `IF`, so every `IF` must have its own matching `ENDIF`.\n\n---\n\n## Key Rules\n\n- Every `IF` needs a matching `ENDIF`\n- To chain conditions, nest a new `IF` inside `ELSE`\n- Indent each level so it is easy to see which branch belongs to which `IF`"
},
{
  title: "CASE Statements",
  content: "# CASE Statements\n\nA CASE statement is a cleaner way to handle multiple options for a single variable. Instead of writing deeply nested IF / ELSE / IF blocks, you list each possible value and what should happen for it.\n\n## Syntax\n\n```\nCASE OF variable\n    value1 : // code for value1\n    value2 : // code for value2\n    value3 : // code for value3\n    OTHERWISE : // code if nothing matched\nENDCASE\n```\n\n- Each line inside lists a **value**, then a colon `:`, then what to do\n- `OTHERWISE` is optional — it runs if none of the values matched, like a final fallback\n- Always end with `ENDCASE`\n\n---\n\n## Example: Day of the Week\n\n```\nDECLARE DayNum : INTEGER\nDayNum <- 3\n\nCASE OF DayNum\n    1 : OUTPUT \"Monday\"\n    2 : OUTPUT \"Tuesday\"\n    3 : OUTPUT \"Wednesday\"\n    4 : OUTPUT \"Thursday\"\n    5 : OUTPUT \"Friday\"\n    OTHERWISE : OUTPUT \"Weekend\"\nENDCASE\n```\n\nOutput:\n```\nWednesday\n```\n\nThe program looks at `DayNum`, finds the matching value (3), and runs just that line. Only one branch ever runs.\n\n---\n\n## Example: Grade Feedback\n\n```\nDECLARE Grade : STRING\nGrade <- \"B\"\n\nCASE OF Grade\n    \"A\" : OUTPUT \"Outstanding!\"\n    \"B\" : OUTPUT \"Well done.\"\n    \"C\" : OUTPUT \"Satisfactory.\"\n    OTHERWISE : OUTPUT \"See your teacher.\"\nENDCASE\n```\n\nOutput:\n```\nWell done.\n```\n\n---\n\n## CASE vs IF\n\nUse a CASE statement when you are checking **one variable** against several exact values. Use IF when your conditions involve ranges or comparisons (like `Score >= 50`).\n\n| Situation | Use |\n|---|---|\n| Checking one variable against exact values | `CASE OF` |\n| Checking ranges or complex conditions | `IF` |\n\n---\n\n## Key Rules\n\n- `CASE OF` is followed by the variable you are checking\n- Each option is written as `value : action`\n- `OTHERWISE :` catches anything that didn't match — always put it last\n- End with `ENDCASE`"
},
{
  title: "Count-Controlled Loops",
  content: "# Count-Controlled Loops (FOR)\n\nA FOR loop repeats a block of code a fixed number of times. You use it when you know exactly how many times you want to repeat something.\n\n## Syntax\n\n```\nFOR variable <- beginning TO end\n    // code to repeat\nNEXT variable\n```\n\n- `variable` counts up from `beginning` to `end`, increasing by 1 each time\n- The code inside runs once for every value of `variable`\n- `NEXT variable` marks the end of the loop and moves to the next count\n\n---\n\n## A Simple Example\n\n```\nFOR Count <- 1 TO 5\n    OUTPUT Count\nNEXT Count\n```\n\nOutput:\n```\n1\n2\n3\n4\n5\n```\n\nThe loop starts with `Count = 1`, runs the body, then increases `Count` to 2, runs again, and so on until `Count` has been 5.\n\n---\n\n## Using the Loop Variable\n\nYou don't have to just output the counter — you can use it in calculations:\n\n```\nFOR i <- 1 TO 5\n    OUTPUT i & \" squared is \" & i * i\nNEXT i\n```\n\nOutput:\n```\n1 squared is 1\n2 squared is 4\n3 squared is 9\n4 squared is 16\n5 squared is 25\n```\n\n---\n\n## Key Rules\n\n- Always declare your loop variable before using it\n- The loop variable increases automatically — you do not need to update it yourself\n- `NEXT` must be followed by the same variable name used after `FOR`\n- If `beginning` is greater than `end`, the loop body does not run at all\n\n---\n\n## Counting in Steps\n\nBy default a `FOR` loop counts up by 1. `STEP` changes the size of the jump:\n\n```\nDECLARE i : INTEGER\n\nFOR i <- 0 TO 10 STEP 2\n    OUTPUT i\nNEXT i\n```\n\nOutput:\n```\n0\n2\n4\n6\n8\n10\n```\n\nA **negative** step counts downwards. The start value then has to be the larger\none:\n\n```\nDECLARE i : INTEGER\n\nFOR i <- 5 TO 1 STEP -1\n    OUTPUT i\nNEXT i\n```\n\nOutput:\n```\n5\n4\n3\n2\n1\n```\n\n> **Careful:** `FOR i <- 1 TO 5 STEP -1` never runs at all — it starts below the\n> end value and walks the wrong way."
},
{
  title: "Totalling and Counting",
  content: "# Totalling and Counting with Loops\n\nTwo of the most common patterns in programming are building up a running total and counting how many times something happens. Both use a loop with a variable that starts at 0.\n\n---\n\n## Totalling\n\nTotalling means adding up values as you go. You declare a variable, set it to 0, then add to it on every iteration of the loop.\n\n### The pattern\n\n```\nDECLARE Total : INTEGER\nTotal <- 0\n\nFOR i <- 1 TO N\n    Total <- Total + i\nNEXT i\n```\n\n`Total` starts at 0. Each time the loop runs, the current value of `i` is added to it.\n\n### Example: Sum 1 to 10\n\n```\nDECLARE Total : INTEGER\nTotal <- 0\n\nFOR i <- 1 TO 10\n    Total <- Total + i\nNEXT i\n\nOUTPUT \"Sum of 1 to 10 is \" & Total\n```\n\nOutput:\n```\nSum of 1 to 10 is 55\n```\n\n### Example: Sum of user inputs\n\n```\nDECLARE Total : INTEGER\nDECLARE Num : INTEGER\nTotal <- 0\n\nFOR i <- 1 TO 5\n    OUTPUT \"Enter a number:\"\n    INPUT Num\n    Total <- Total + Num\nNEXT i\n\nOUTPUT \"Total is \" & Total\n```\n\nThe loop asks for 5 numbers one at a time and adds each one to `Total`.\n\n---\n\n## Counting\n\nCounting means adding a **fixed constant** to a variable each time something happens — most commonly `1`, but it could be `2`, `5`, or any value that doesn't change. The key difference from totalling is that you are not adding the data value itself — you are adding a constant, usually only when a condition is met.\n\n### The pattern\n\n```\nDECLARE Count : INTEGER\nCount <- 0\n\nFOR i <- 1 TO N\n    IF // some condition THEN\n        Count <- Count + 1\n    ENDIF\nNEXT i\n```\n\n### Example: Count positive numbers\n\n```\nDECLARE Count : INTEGER\nDECLARE Num : INTEGER\nCount <- 0\n\nFOR i <- 1 TO 10\n    OUTPUT \"Enter a number:\"\n    INPUT Num\n    IF Num > 0 THEN\n        Count <- Count + 1\n    ENDIF\nNEXT i\n\nOUTPUT \"You entered \" & Count & \" positive numbers.\"\n```\n\n`Count` starts at 0. Every time the user enters a positive number, `1` is added to it. You could equally add `2` each time to count in steps of two, or any other constant — the value added never depends on the data.\n\n---\n\n## Key Points\n\n- Always initialise your total or counter variable to `0` before the loop\n- Totalling: `Total <- Total + value` on every iteration\n- Counting: `Count <- Count + 1` only when the condition is met\n- Both patterns work with any type of loop — FOR, WHILE, or REPEAT"
},
{
  title: "Pre-Condition Loops",
  content: "# Pre-Condition Loops (WHILE)\n\nA WHILE loop repeats a block of code **as long as a condition is true**. Unlike a FOR loop, you don't say how many times it should run — it keeps going until the condition becomes false.\n\nThe condition is checked **before** each iteration. If it's false from the start, the loop body never runs at all.\n\n## Syntax\n\n```\nWHILE condition\n    // code to repeat\nENDWHILE\n```\n\n---\n\n## A Simple Example\n\n```\nDECLARE Count : INTEGER\nCount <- 1\n\nWHILE Count <= 5\n    OUTPUT Count\n    Count <- Count + 1\nENDWHILE\n```\n\nOutput:\n```\n1\n2\n3\n4\n5\n```\n\nNotice that **you** are responsible for updating `Count` inside the loop. If you forget, the condition will never become false and the loop will run forever.\n\n---\n\n## When to Use WHILE Instead of FOR\n\nUse a WHILE loop when you don't know in advance how many times the loop needs to run.\n\nFor example, keep asking the user for a password until they get it right:\n\n```\nDECLARE Password : STRING\nINPUT Password\n\nWHILE Password <> \"secret123\"\n    OUTPUT \"Wrong password. Try again:\"\n    INPUT Password\nENDWHILE\n\nOUTPUT \"Access granted.\"\n```\n\nYou can't use a FOR loop here because you have no idea how many attempts the user will need.\n\n---\n\n## The Condition is Checked First\n\nBecause the condition is checked before the body runs, it's possible the loop runs zero times:\n\n```\nDECLARE x : INTEGER\nx <- 10\n\nWHILE x < 5\n    OUTPUT x\nENDWHILE\n\nOUTPUT \"Done\"\n```\n\nOutput:\n```\nDone\n```\n\nSince `x` is already 10, the condition `x < 5` is false immediately, so the body is skipped entirely.\n\n---\n\n## Key Rules\n\n- The condition is checked **before** each run of the loop body\n- You must update whatever is in the condition inside the loop, or it will loop forever\n- Always end with `ENDWHILE`\n- Use WHILE when the number of repetitions is not known in advance"
},
{
  title: "Post-Condition Loops",
  content: "# Post-Condition Loops (REPEAT)\n\nA REPEAT loop is similar to a WHILE loop, but with one key difference — the condition is checked **after** the body runs, not before.\n\nThis means the loop body **always runs at least once**, no matter what.\n\n## Syntax\n\n```\nREPEAT\n    // code to repeat\nUNTIL condition\n```\n\nThe loop keeps running until the condition becomes **true**. It stops when the condition is met.\n\n> Note: This is the opposite of WHILE, which runs while the condition is true. REPEAT runs until the condition becomes true.\n\n---\n\n## A Simple Example\n\n```\nDECLARE Count : INTEGER\nCount <- 1\n\nREPEAT\n    OUTPUT Count\n    Count <- Count + 1\nUNTIL Count > 5\n```\n\nOutput:\n```\n1\n2\n3\n4\n5\n```\n\nThe body runs, then `Count > 5` is checked. It keeps going until `Count` reaches 6, at which point the condition is true and the loop stops.\n\n---\n\n## The Key Use Case: Input Validation\n\nREPEAT is perfect for asking a user for input and making sure it's valid. Since you always need to ask at least once, the body running at least once makes sense:\n\n```\nDECLARE Age : INTEGER\n\nREPEAT\n    OUTPUT \"Enter your age (must be positive):\"\n    INPUT Age\nUNTIL Age > 0\n```\n\nThe program asks for an age, then checks if it's valid. If not, it asks again. It keeps going until the user enters a positive number.\n\n---\n\n## REPEAT vs WHILE\n\n| | WHILE | REPEAT |\n|---|---|---|\n| Condition checked | Before the body | After the body |\n| Minimum runs | 0 (might never run) | 1 (always runs once) |\n| Stops when | Condition becomes false | Condition becomes true |\n\n---\n\n## Key Rules\n\n- The body always runs at least once\n- The loop stops when the condition after `UNTIL` becomes **true**\n- Like WHILE, you must update whatever is in the condition inside the loop\n- Use REPEAT when you always need the body to run at least once — especially for input validation"
},
{
  title: "Validation",
  content: "# Validation\n\n**Validation** is an automatic check a program makes on data as it is entered,\nto decide whether the data is *reasonable* before the program uses it.\n\nValidation cannot tell you data is **correct** — only that it is sensible. If\nsomeone types the age `42` when they meant `24`, every validation check in the\nworld will happily accept it. Checking that data is genuinely correct is called\n**verification**, and it usually means asking a human to confirm it or entering\nit twice.\n\nEvery check below follows the same shape: a `REPEAT` loop that keeps asking\nuntil the data passes. That works because a post-condition loop always runs at\nleast once — you have to ask before you can check.\n\n---\n\n## Range Check\n\nIs the number between sensible limits?\n\n```\nDECLARE Age : INTEGER\n\nREPEAT\n    OUTPUT \"Enter your age (0 to 120):\"\n    INPUT Age\nUNTIL Age >= 0 AND Age <= 120\n\nOUTPUT \"Accepted: \", Age\n```\n\n---\n\n## Length Check\n\nDoes the text have an acceptable number of characters?\n\n```\nDECLARE Password : STRING\n\nREPEAT\n    OUTPUT \"Enter a password of at least 8 characters:\"\n    INPUT Password\nUNTIL LENGTH(Password) >= 8\n\nOUTPUT \"Password accepted\"\n```\n\n---\n\n## Presence Check\n\nWas anything entered at all?\n\n```\nDECLARE Name : STRING\n\nREPEAT\n    OUTPUT \"Enter your name (it cannot be blank):\"\n    INPUT Name\nUNTIL Name <> \"\"\n\nOUTPUT \"Hello, \", Name\n```\n\n---\n\n## Type Check\n\nIs the data of the right kind? `IS_NUM` reports whether a string holds a number.\n\n```\nDECLARE Entry : STRING\n\nREPEAT\n    OUTPUT \"Enter a number:\"\n    INPUT Entry\nUNTIL IS_NUM(Entry)\n\nOUTPUT \"That is a number: \", Entry\n```\n\n---\n\n## Format Check\n\nDoes the data follow a required pattern? A product code of one letter followed\nby three digits is four characters long, and characters 2 to 4 must be numeric:\n\n```\nDECLARE Code : STRING\nDECLARE Valid : BOOLEAN\n\nREPEAT\n    OUTPUT \"Enter a product code (letter then 3 digits, e.g. A123):\"\n    INPUT Code\n    Valid <- FALSE\n    IF LENGTH(Code) = 4 THEN\n        IF IS_NUM(SUBSTRING(Code, 2, 3)) THEN\n            Valid <- TRUE\n        ENDIF\n    ENDIF\nUNTIL Valid = TRUE\n\nOUTPUT \"Code accepted: \", Code\n```\n\n---\n\n## Choosing Test Data\n\nOnce a check is written you have to prove it works, and that means choosing\ninput deliberately rather than typing whatever comes to mind. For the age range\ncheck above:\n\n| Kind of data | Meaning | Example |\n|---|---|---|\n| **Normal** | Data that should be accepted | `17` |\n| **Abnormal** | Data that should be rejected | `-5`, `abc` |\n| **Extreme** | Largest and smallest values still accepted | `0`, `120` |\n| **Boundary** | The pair either side of a limit | `120` accepted, `121` rejected |\n\n> **Boundary data is where the marks are.** Most faulty range checks use `>`\n> where they need `>=`, and only boundary data ever finds it."
},
{
  title: "Procedures",
  content: "# Procedures\n\nImagine you have a friend and you tell them: **\"Go make me a cup of tea.\"** They go off, do all the steps involved, and come back. They didn't hand you anything — they just carried out a task.\n\nThat's exactly what a procedure is. A **procedure** is a named block of code that carries out a task. You call it by name, it runs, and then the program continues. It does not give you a value back.\n\n---\n\n## Why Use Procedures?\n\nIf you find yourself writing the same block of code more than once, put it in a procedure. You write it once, give it a name, and call it whenever you need it.\n\n---\n\n## Defining a Procedure\n\n```\nPROCEDURE PrintDivider()\n    OUTPUT \"-------------------\"\nENDPROCEDURE\n```\n\nThis defines a procedure called `PrintDivider`. On its own, this does nothing yet — you still need to call it.\n\n## Calling a Procedure\n\n```\nCALL PrintDivider()\nOUTPUT \"Welcome\"\nCALL PrintDivider()\n```\n\nOutput:\n```\n-------------------\nWelcome\n-------------------\n```\n\n---\n\n## Procedures with Parameters\n\nParameters let you pass information into a procedure so it can work with different values each time.\n\nThink of it like telling your friend: **\"Go make me a cup of tea — two sugars.\"** The \"two sugars\" is the parameter — it changes what happens inside.\n\n```\nPROCEDURE Greet(Name : STRING)\n    OUTPUT \"Hello, \" & Name & \"!\"\nENDPROCEDURE\n```\n\nCalling it:\n\n```\nCALL Greet(\"Alice\")\nCALL Greet(\"Bob\")\n```\n\nOutput:\n```\nHello, Alice!\nHello, Bob!\n```\n\nThe same procedure runs twice but produces different output each time because a different value was passed in.\n\n---\n\n## Multiple Parameters\n\nYou can have more than one parameter, separated by commas:\n\n```\nPROCEDURE PrintSum(A : INTEGER, B : INTEGER)\n    OUTPUT A & \" + \" & B & \" = \" & (A + B)\nENDPROCEDURE\n\nCALL PrintSum(3, 5)\nCALL PrintSum(10, 20)\n```\n\nOutput:\n```\n3 + 5 = 8\n10 + 20 = 30\n```\n\n---\n\n## Key Rules\n\n- Define a procedure with `PROCEDURE` ... `ENDPROCEDURE`\n- Call it with `CALL ProcedureName()`\n- A procedure does **not** return a value — it just carries out actions\n- Parameters go inside the brackets with their types declared"
},
{
  title: "Functions",
  content: "# Functions\n\nThink of a function like a **vending machine**. You put something in (your selection), and it gives you something back (a snack). You always get a result out of it.\n\nThat's the key difference from a procedure: a **function** always returns a value. You use it when you need the program to calculate or produce something you can then use.\n\n---\n\n## Defining a Function\n\n```\nFUNCTION Double(n : INTEGER) RETURNS INTEGER\n    RETURN n * 2\nENDFUNCTION\n```\n\n- `RETURNS INTEGER` tells the program what type of value comes back\n- `RETURN` sends the value back to wherever the function was called from\n\n## Calling a Function\n\nYou call a function by assigning its result to a variable:\n\n```\nDECLARE Result : INTEGER\nResult <- Double(5)\nOUTPUT Result\n```\n\nOutput:\n```\n10\n```\n\nYou can also use it directly in OUTPUT:\n\n```\nOUTPUT Double(7)\n```\n\nOutput:\n```\n14\n```\n\n---\n\n## A More Useful Example\n\n```\nFUNCTION Max(A : INTEGER, B : INTEGER) RETURNS INTEGER\n    IF A > B THEN\n        RETURN A\n    ELSE\n        RETURN B\n    ENDIF\nENDFUNCTION\n\nOUTPUT Max(10, 25)\nOUTPUT Max(42, 7)\n```\n\nOutput:\n```\n25\n42\n```\n\nThe function takes two numbers and returns whichever is larger.\n\n---\n\n## Functions vs Procedures — What's the Difference?\n\nThis is the part people often mix up. Here's a simple way to think about it:\n\n| | Procedure | Function |\n|---|---|---|\n| Returns a value? | No | Yes |\n| How you call it | `CALL Name()` | `Result <- Name()` |\n| Use it when… | You want to carry out an action | You want to calculate and get a value back |\n\n**Procedure:** \"Go print the divider line.\" — just do something.\n\n**Function:** \"What is the largest of these two numbers?\" — give me an answer.\n\n---\n\n## Key Rules\n\n- Define a function with `FUNCTION` ... `ENDFUNCTION`\n- Always declare what type it returns with `RETURNS TYPE`\n- Use `RETURN value` inside the function to send the result back\n- Call it by assigning its result: `Variable <- FunctionName(args)`\n- A function **must** always reach a `RETURN` statement"
},
{
  title: "String Manipulation",
  content: "# String Manipulation\n\nStrings aren't just for storing text — you can also measure their length and extract parts of them. This lesson covers these two essential string operations.\n\n---\n\n## String Length\n\n`LENGTH` tells you how many characters are in a string.\n\n```\nDECLARE Word : STRING\nWord <- \"Cambridge\"\n\nOUTPUT LENGTH(Word)\n```\n\nOutput:\n```\n9\n```\n\nSpaces count as characters too:\n\n```\nOUTPUT LENGTH(\"Hello World\")\n```\n\nOutput:\n```\n11\n```\n\n---\n\n## Getting a Substring\n\n`SUBSTRING` lets you extract a portion of a string. It takes three things:\n\n```\nSUBSTRING(StrVariable, StartIndex, Length)\n```\n\n| Parameter | Meaning |\n|---|---|\n| `StrVariable` | The string to extract from |\n| `StartIndex` | The position to start at (1 = first character) |\n| `Length` | How many characters to extract |\n\n### Example\n\n```\nDECLARE Word : STRING\nWord <- \"Cambridge\"\n\nOUTPUT SUBSTRING(Word, 1, 3)\nOUTPUT SUBSTRING(Word, 4, 6)\n```\n\nOutput:\n```\nCam\nbridge\n```\n\nThink of it like cutting a piece of ribbon — you say where to start cutting and how long the piece should be.\n\n### Extracting a single character\n\nTo get just one character at position `i`, use a length of 1:\n\n```\nOUTPUT SUBSTRING(Word, 1, 1)\n```\n\nOutput:\n```\nC\n```\n\n---\n\n## Putting It Together\n\n```\nDECLARE FullName : STRING\nFullName <- \"Alice Smith\"\n\nOUTPUT \"Full name:  \" & FullName\nOUTPUT \"Length:     \" & LENGTH(FullName)\nOUTPUT \"First name: \" & SUBSTRING(FullName, 1, 5)\n```\n\nOutput:\n```\nFull name:  Alice Smith\nLength:     11\nFirst name: Alice\n```\n\n---\n\n## Changing Case\n\n`UCASE` converts a character to upper case and `LCASE` converts it to lower case.\n\nIn this dialect — following section 5.5 of the Cambridge guide — both take a\nsingle **CHAR**, not a whole STRING:\n\n```\nDECLARE Letter : CHAR\n\nLetter <- 'g'\nOUTPUT UCASE(Letter)\n\nLetter <- 'G'\nOUTPUT LCASE(Letter)\n```\n\nOutput:\n```\nG\ng\n```\n\n> **Watch out:** `SUBSTRING` always returns a STRING, even when you ask for one\n> character, and a STRING cannot be handed to `UCASE`. To change the case of\n> text you have taken out of a string, compare each character against an\n> alphabet and swap it — the closing challenge of this course shows one way."
},
{
  title: "1D Arrays",
  content: "# 1D Arrays\n\nImagine a **row of labelled lockers** in a school corridor. Each locker holds one item, and every locker has a number on it. You can open any locker directly if you know its number — you don't have to open them all one by one.\n\nThat's exactly what an **array** is: a fixed-length row of storage slots, each holding one value of the same type, each reachable by its index number.\n\n---\n\n## Declaring an Array\n\n```\nDECLARE Scores : ARRAY[1:5] OF INTEGER\n```\n\nThis creates **5 integer slots**, numbered 1 to 5.\n\n| Part | Meaning |\n|---|---|\n| `Scores` | The name of the array |\n| `[1:5]` | Slots numbered 1 through 5 |\n| `OF INTEGER` | Every slot holds an integer |\n\n> **Note:** By convention, arrays always start at index **1** in Cambridge Pseudocode.\n\n---\n\n## Assigning Values\n\nUse the index in square brackets to put a value into a slot:\n\n```\nScores[1] <- 85\nScores[2] <- 72\nScores[3] <- 91\nScores[4] <- 60\nScores[5] <- 78\n```\n\nThink of it as: *\"Put 85 into locker number 1.\"*\n\n---\n\n## Reading Values\n\nYou read a slot the same way — just use the index:\n\n```\nOUTPUT Scores[3]\n```\n\nOutput:\n```\n91\n```\n\nYou can also assign a slot's value to a variable:\n\n```\nDECLARE Best : INTEGER\nBest <- Scores[3]\nOUTPUT \"Best score: \" & Best\n```\n\n---\n\n## Looping Through an Array\n\nThis is where arrays become powerful. Because slots are numbered, you can use a `FOR` loop to visit every one:\n\n```\nFOR I <- 1 TO 5\n    OUTPUT Scores[I]\nNEXT I\n```\n\nThe variable `I` acts like a finger sliding along the lockers — pointing at slot 1, then 2, then 3, and so on.\n\n---\n\n## Practical Example — Filling and Totalling\n\n```\nDECLARE Scores : ARRAY[1:5] OF INTEGER\nDECLARE Total : INTEGER\nDECLARE Average : REAL\n\nTotal <- 0\n\nFOR I <- 1 TO 5\n    OUTPUT \"Enter score \" & I & \":\"\n    INPUT Scores[I]\n    Total <- Total + Scores[I]\nNEXT I\n\nAverage <- Total / 5\nOUTPUT \"Average score: \" & Average\n```\n\nNotice that `INPUT Scores[I]` fills each slot directly — no need for a separate variable.\n\n---\n\n## Key Rules\n\n- Declare with `ARRAY[Start:End] OF Type`\n- Always start at index **1** by convention\n- All slots must hold the **same type**\n- Access any slot instantly with `ArrayName[Index]`\n- The index can be a variable — which makes `FOR` loops a natural fit"
},
{
  title: "2D Arrays",
  content: "# 2D Arrays\n\nA 1D array is a single row of lockers. A **2D array** is a whole **grid** of lockers — like a spreadsheet, or a seating plan in a classroom. Every cell has a **row** number and a **column** number, and you need both to find it.\n\n---\n\n## Declaring a 2D Array\n\n```\nDECLARE Grid : ARRAY[1:3, 1:4] OF INTEGER\n```\n\nThis creates a grid with **3 rows** and **4 columns** — 12 slots in total.\n\n```\n        Col 1   Col 2   Col 3   Col 4\nRow 1  [  ?  ] [  ?  ] [  ?  ] [  ?  ]\nRow 2  [  ?  ] [  ?  ] [  ?  ] [  ?  ]\nRow 3  [  ?  ] [  ?  ] [  ?  ] [  ?  ]\n```\n\nThe format is always `[StartRow:EndRow, StartCol:EndCol]`.\n\n---\n\n## Accessing a Cell\n\nUse `ArrayName[Row, Col]` to read or write a specific cell:\n\n```\nGrid[1, 1] <- 10\nGrid[1, 2] <- 20\nGrid[2, 3] <- 99\n```\n\nThink of it like giving directions: *\"Row 2, Column 3\"* — just like map coordinates.\n\n```\nOUTPUT Grid[2, 3]\n```\n\nOutput:\n```\n99\n```\n\n---\n\n## Looping Through a 2D Array\n\nTo visit every cell you need **two nested FOR loops** — one for rows, one for columns:\n\n```\nDECLARE Rows : INTEGER\nDECLARE Cols : INTEGER\nRows <- 3\nCols <- 4\n\nFOR Row <- 1 TO Rows\n    FOR Col <- 1 TO Cols\n        OUTPUT Grid[Row, Col]\n    NEXT Col\nNEXT Row\n```\n\nThe outer loop moves down the rows; the inner loop sweeps across each column within that row — like reading a book line by line.\n\n---\n\n## Practical Example — Filling a Grid with User Input\n\n```\nDECLARE Marks : ARRAY[1:3, 1:2] OF INTEGER\n\nFOR Row <- 1 TO 3\n    FOR Col <- 1 TO 2\n        OUTPUT \"Enter mark for row \" & Row & \", col \" & Col & \":\"\n        INPUT Marks[Row, Col]\n    NEXT Col\nNEXT Row\n```\n\n---\n\n## Practical Example — Finding the Largest Value\n\n```\nDECLARE Largest : INTEGER\nLargest <- Grid[1, 1]\n\nFOR Row <- 1 TO 3\n    FOR Col <- 1 TO 4\n        IF Grid[Row, Col] > Largest THEN\n            Largest <- Grid[Row, Col]\n        ENDIF\n    NEXT Col\nNEXT Row\n\nOUTPUT \"Largest value: \" & Largest\n```\n\n---\n\n## Key Rules\n\n- Declare with `ARRAY[StartRow:EndRow, StartCol:EndCol] OF Type`\n- Access any cell with `ArrayName[Row, Col]`\n- Both dimensions start at **1** by convention\n- Use **nested FOR loops** to process every cell — outer loop for rows, inner loop for columns"
},
{
  title: "Linear Search",
  content: "# Linear Search\n\nSearching is one of the most common things a program does. **Linear search** is the simplest approach — check each element one by one, from the beginning, until you find what you're looking for (or run out of elements to check).\n\n---\n\n## The Analogy\n\nImagine you've dropped your keys somewhere in your house. You check room by room, starting from the front door — kitchen, living room, bedroom… You stop the moment you find them. If you reach the last room and they're not there, they aren't in the house.\n\nThat's linear search. No shortcuts, no assumptions — just check everything in order.\n\n---\n\n## Searching Through an Array\n\nSay we want to find the value **3** in this array:\n\n```\nIndex:  1    2    3    4    5\n      [ 7 ][ 2 ][ 3 ][ 9 ][ 1 ]\n```\n\n**Step 1** — Check index 1:\n```\n      [ 7 ][ 2 ][ 3 ][ 9 ][ 1 ]\n        ^\n        7 = 3? No.\n```\n\n**Step 2** — Check index 2:\n```\n      [ 7 ][ 2 ][ 3 ][ 9 ][ 1 ]\n             ^\n             2 = 3? No.\n```\n\n**Step 3** — Check index 3:\n```\n      [ 7 ][ 2 ][ 3 ][ 9 ][ 1 ]\n                  ^\n                  3 = 3? YES — found at index 3!\n```\n\nWe return **3** (the index where we found it) and stop immediately.\n\n---\n\n## What If It's Not There?\n\nSearching for **5** in the same array:\n\n```\n      [ 7 ][ 2 ][ 3 ][ 9 ][ 1 ]\n        ^    ^    ^    ^    ^\n        No   No   No   No   No  → not found\n```\n\nWe reach the end without finding it. We return **-1** to signal \"not found\".\n\n---\n\n## The Code\n\n```\nFUNCTION LinearSearch(Nums : ARRAY[1:ArraySize] OF INTEGER, Target : INTEGER) RETURNS INTEGER\n    FOR Idx <- 1 TO ArraySize\n        IF Nums[Idx] = Target THEN\n            RETURN Idx\n        ENDIF\n    NEXT Idx\n    RETURN -1\nENDFUNCTION\n```\n\n### How to use it\n\n```\nDECLARE Position : INTEGER\nPosition <- LinearSearch(Nums, 3)\n\nIF Position <> -1 THEN\n    OUTPUT \"Found at index \" & Position\nELSE\n    OUTPUT \"Not found\"\nENDIF\n```\n\n---\n\n## Walking Through the Code\n\n### The FOR loop\n\n```\nFOR Idx <- 1 TO ArraySize\n```\n\n`Idx` starts at 1 and moves through every index in the array — like the pointer moving from slot to slot in the diagram above.\n\n### The check\n\n```\nIF Nums[Idx] = Target THEN\n    RETURN Idx\nENDIF\n```\n\nThe moment we find a match, we immediately `RETURN` the index. The function stops — we don't need to check the rest.\n\n### The fallback\n\n```\nRETURN -1\n```\n\nIf the loop finishes without finding anything, we return `-1` to signal \"not found\".\n\n---\n\n## Best Case vs Worst Case\n\nThe position of the target affects how long the search takes:\n\n```\n[ 3 ][ 7 ][ 2 ][ 9 ][ 1 ]   ← target is at index 1: found in 1 check  (best case)\n[ 7 ][ 2 ][ 9 ][ 1 ][ 3 ]   ← target is at index 5: found in 5 checks (worst case)\n[ 7 ][ 2 ][ 9 ][ 4 ][ 1 ]   ← target not in array:  checked all 5     (worst case)\n```\n\nIn the worst case, every single element is checked. For small arrays this is fine. For very large arrays, more efficient algorithms exist — but linear search is always correct, and sometimes that's all you need.\n\n---\n\n## Key Points\n\n- Linear search checks each element **one by one** from start to end\n- It stops and returns the index the **moment** a match is found\n- If nothing is found after checking everything, it returns **0**\n- It works on **any array** — the values do not need to be sorted\n- `ArraySize` must be declared as a global variable before calling this function"
},
{
  title: "Bubble Sort",
  content: "# Bubble Sort\n\nBubble sort is one of the most important algorithms to understand. The idea is simple, but it trips people up when they first see it — so let's go through it very carefully.\n\n---\n\n## The Analogy\n\nImagine a line of people of different heights, and you want to sort them tallest on the right. You walk along the line comparing pairs of neighbours — if the left person is taller than the right, they swap. You repeat this until no one needs to swap.\n\nThe tallest person gradually \"bubbles\" to the right end on the first pass, the second tallest on the second pass, and so on.\n\n---\n\n## The Core Idea — Comparing Neighbours\n\nGiven this array:\n\n```\nIndex:  1    2    3    4\n      [ 4 ][ 2 ][ 3 ][ 1 ]\n```\n\nWe compare **Nums[1] and Nums[2]**. Is 4 > 2? Yes — swap them:\n\n```\n      [ 2 ][ 4 ][ 3 ][ 1 ]\n            ^\n            4 moved right\n```\n\nNow compare **Nums[2] and Nums[3]**. Is 4 > 3? Yes — swap:\n\n```\n      [ 2 ][ 3 ][ 4 ][ 1 ]\n                  ^\n                  4 moved right again\n```\n\nNow compare **Nums[3] and Nums[4]**. Is 4 > 1? Yes — swap:\n\n```\n      [ 2 ][ 3 ][ 1 ][ 4 ]\n                       ^\n                       4 is now in its final place!\n```\n\nAfter one full pass, the **largest value has bubbled all the way to the end**. We never need to touch it again.\n\n---\n\n## Pass 2\n\n```\n      [ 2 ][ 3 ][ 1 ][ 4 ]\n                        ✓ already sorted, skip\n```\n\nCompare Nums[1] and Nums[2]: 2 > 3? No — no swap.\nCompare Nums[2] and Nums[3]: 3 > 1? Yes — swap:\n\n```\n      [ 2 ][ 1 ][ 3 ][ 4 ]\n                  ^    ^\n                  both in final place!\n```\n\n---\n\n## Pass 3\n\n```\n      [ 2 ][ 1 ][ 3 ][ 4 ]\n                  ✓    ✓\n```\n\nCompare Nums[1] and Nums[2]: 2 > 1? Yes — swap:\n\n```\n      [ 1 ][ 2 ][ 3 ][ 4 ]   ← fully sorted!\n```\n\nBecause a swap happened, we can't be sure it's done — so we do one more pass. On that pass **nothing swaps**, so `Sorted` stays `TRUE` and we stop.\n\n---\n\n## The Code — Step by Step\n\n```\nFUNCTION BubbleSort(Nums : ARRAY[1:ArraySize] OF INTEGER) RETURNS ARRAY[1:ArraySize] OF INTEGER\n    DECLARE Sorted : BOOLEAN\n    DECLARE Count : INTEGER\n    DECLARE Temp : INTEGER\n\n    Sorted <- FALSE\n    Count <- 0\n    Temp <- 0\n\n    REPEAT\n        Sorted <- TRUE\n\n        FOR Idx <- 1 TO ArraySize - Count - 1\n            IF Nums[Idx] > Nums[Idx + 1] THEN\n                Temp <- Nums[Idx]\n                Nums[Idx] <- Nums[Idx + 1]\n                Nums[Idx + 1] <- Temp\n                Sorted <- FALSE\n            ENDIF\n        NEXT Idx\n    UNTIL Sorted\n\n    RETURN Nums\n\nENDFUNCTION\n```\n\n### `Sorted <- TRUE` at the start of each pass\n\nWe **assume** the array is sorted before each pass. If we get through the whole pass without swapping anything, that assumption was correct — we're done.\n\n### `Sorted <- FALSE` inside the swap\n\nThe moment we swap something, we know the array wasn't sorted yet, so we set `Sorted` back to `FALSE` — meaning we'll do at least one more pass.\n\n### `UNTIL Sorted`\n\nThe REPEAT loop stops as soon as a full pass completes with no swaps. That's the signal that everything is in order.\n\n### Why `ArraySize - Count - 1`?\n\nAfter each pass, the largest unsorted value has settled at the end. We don't need to compare it again. `Count` tracks how many values are already in their final place, so we shorten the inner loop each time:\n\n```\nPass 1 (Count=0): compare positions 1→3   (4 - 0 - 1 = 3)\nPass 2 (Count=1): compare positions 1→2   (4 - 1 - 1 = 2)\nPass 3 (Count=2): compare positions 1→1   (4 - 2 - 1 = 1)\n```\n\n### The Swap — why do we need `Temp`?\n\nYou can't just write `Nums[Idx] <- Nums[Idx+1]` because that would destroy the original value before you've saved it. `Temp` holds the value while you move things around — like putting one item on the floor while you rearrange two shelves:\n\n```\nTemp         <- Nums[Idx]       // put Nums[Idx] aside\nNums[Idx]    <- Nums[Idx + 1]  // overwrite it safely\nNums[Idx+1]  <- Temp            // place the saved value\n```\n\n---\n\n## Key Points\n\n- Bubble sort compares **adjacent pairs** and swaps them if they're in the wrong order\n- After each pass, the next-largest value is **guaranteed to be in its final position**\n- The `Sorted` flag gives an **early exit** — if no swaps happen, the array is already sorted\n- `Count` **shrinks the inner loop** each pass so we don't re-check already-sorted positions\n- Always need a `Temp` variable to swap two values without losing one\n- `ArraySize` must be declared as a global variable before calling this function"
},
{
  title: "File Handling",
  content: "# File Handling\n\nSo far, everything your programs store disappears the moment they stop running. **Files** let you save data so it survives — and read it back later.\n\nThink of a file like a **notepad**. You can open it, read what's written, write new things, or add to the bottom. When you're done, you close it.\n\n---\n\n## Opening and Closing a File\n\nBefore you do anything with a file, you must **open** it. When you're done, you must **close** it.\n\n```\nOPENFILE \"notes.txt\" FOR WRITE\nCLOSEFILE \"notes.txt\"\n```\n\nThe filename is always a **string** (in quotes, or a STRING variable). You choose a **mode** depending on what you want to do.\n\n---\n\n## The Three Modes\n\n| Mode | What it does |\n|---|---|\n| `READ` | Opens the file so you can read from it |\n| `WRITE` | Opens the file for writing — **clears all existing content first**. If the file doesn't exist, it is created. |\n| `APPEND` | Opens the file for writing — but **adds to the end** without removing anything |\n\n> **Important:** WRITE wipes the file clean before writing. If you want to keep what's already there and just add more, use APPEND.\n\n---\n\n## Writing to a File\n\n```\nOPENFILE \"scores.txt\" FOR WRITE\nWRITEFILE \"scores.txt\", \"Alice: 85\"\nWRITEFILE \"scores.txt\", \"Bob: 72\"\nCLOSEFILE \"scores.txt\"\n```\n\nEach `WRITEFILE` writes one line and then moves to the **next line** automatically. So the file will contain:\n\n```\nAlice: 85\nBob: 72\n```\n\n---\n\n## Appending to a File\n\n```\nOPENFILE \"scores.txt\" FOR APPEND\nWRITEFILE \"scores.txt\", \"Charlie: 91\"\nCLOSEFILE \"scores.txt\"\n```\n\nThe file now contains:\n\n```\nAlice: 85\nBob: 72\nCharlie: 91\n```\n\n---\n\n## Reading from a File\n\n`READFILE` reads **one line at a time** into a variable, then moves to the next line ready for the next read.\n\n```\nDECLARE Line : STRING\n\nOPENFILE \"scores.txt\" FOR READ\nREADFILE \"scores.txt\", Line\nOUTPUT Line\nCLOSEFILE \"scores.txt\"\n```\n\nOutput:\n```\nAlice: 85\n```\n\n---\n\n## Reading the Whole File with EOF\n\nYou rarely know in advance how many lines a file has. `EOF(filename)` returns `TRUE` when you have reached the **end of the file**, so you can use a `WHILE` loop to read every line:\n\n```\nDECLARE Line : STRING\n\nOPENFILE \"scores.txt\" FOR READ\nWHILE NOT EOF(\"scores.txt\")\n    READFILE \"scores.txt\", Line\n    OUTPUT Line\nENDWHILE\nCLOSEFILE \"scores.txt\"\n```\n\nThis keeps reading lines until there are no more left. `EOF` only works in `READ` mode.\n\n---\n\n## Key Rules\n\n- Always `OPENFILE` before reading or writing, and `CLOSEFILE` when done\n- `WRITE` clears the file first — use `APPEND` to add without erasing\n- `WRITE` creates the file if it doesn't exist\n- Each `READFILE` or `WRITEFILE` moves to the next line automatically\n- Use `WHILE NOT EOF(filename)` to read a whole file of unknown length"
},
{
  title: "Putting it All Together",
  content: "# Putting it All Together\n\nYou've covered everything — variables, loops, conditions, functions, procedures, arrays, string manipulation, and file handling. Now it's time to use all of it in one program.\n\n---\n\n## The Challenge — Sprint Time Tracker\n\nWrite a program that records and analyses an athlete's sprint times. It should:\n\n1. Ask for the athlete's **name** and convert it to uppercase with `UCASE`\n2. Use a **FOR loop** to collect **5 sprint times** (in seconds) and store them in an array, keeping a running total as you go\n3. Use a second **FOR loop** to find the **best (lowest) time**\n4. Calculate the **average time** from the total\n5. Use a **function** to rate the athlete based on their best time:\n   - Under 60 seconds → `\"Excellent\"`\n   - 60–89 seconds → `\"Good\"`\n   - 90 or above → `\"Keep Training\"`\n6. Use a **procedure** to print a divider line (`\"========================\"`) and call it before and after the report\n7. Open `\"report.txt\"` in `WRITE` mode and save the athlete's name, best time, average, and rating — one line each\n8. Read `\"report.txt\"` back using `WHILE NOT EOF` and print every line\n\n---\n\n## Expected Output\n\nFor athlete **Alice**, times **72, 68, 75, 65, 70**:\n\n```\nEnter athlete name:\nEnter time for run 1 (seconds):\nEnter time for run 2 (seconds):\nEnter time for run 3 (seconds):\nEnter time for run 4 (seconds):\nEnter time for run 5 (seconds):\n========================\nAthlete: ALICE\nBest time: 65s\nAverage:   70s\nRating:    Good\n========================\n--- Saved Report ---\nAthlete: ALICE\nBest time: 65s\nAverage: 70s\nRating: Good\n```\n\n---\n\n## Checklist\n\n- [ ] Used `UCASE` to convert the athlete's name\n- [ ] Declared an array `ARRAY[1:5] OF INTEGER` for the times\n- [ ] Used a `FOR` loop to fill the array and accumulate the total\n- [ ] Used a second `FOR` loop with an `IF` to find the best time\n- [ ] Calculated the average using division\n- [ ] Wrote a `FUNCTION` that returns a rating string using nested `IF / ELSE`\n- [ ] Wrote a `PROCEDURE` that outputs a divider line and called it twice\n- [ ] Wrote the report to a file using `OPENFILE / WRITEFILE / CLOSEFILE`\n- [ ] Read the file back using `WHILE NOT EOF`\n\n> **Tip:** Write the function and procedure first. Then tackle the main program in order: collect → find best → calculate → display → save → read back.",
  solution: "FUNCTION GetRating(BestTime : INTEGER) RETURNS STRING\n    IF BestTime < 60 THEN\n        RETURN \"Excellent\"\n    ELSE\n        IF BestTime < 90 THEN\n            RETURN \"Good\"\n        ELSE\n            RETURN \"Keep Training\"\n        ENDIF\n    ENDIF\nENDFUNCTION\n\nPROCEDURE PrintDivider()\n    OUTPUT \"========================\"\nENDPROCEDURE\n\nDECLARE Name : STRING\nDECLARE Times : ARRAY[1:5] OF INTEGER\nDECLARE Total : INTEGER\nDECLARE Best : INTEGER\nDECLARE Average : REAL\nDECLARE Rating : STRING\nDECLARE Line : STRING\n\nOUTPUT \"Enter athlete name:\"\nINPUT Name\n\n// UCASE takes a CHAR in this dialect (guide 5.5) and SUBSTRING yields a\n// STRING, so the name is upper-cased by looking each letter up instead.\nDECLARE Lower : STRING\nDECLARE Upper : STRING\nDECLARE Ch : STRING\nDECLARE UpperName : STRING\nDECLARE K : INTEGER\nLower <- \"abcdefghijklmnopqrstuvwxyz\"\nUpper <- \"ABCDEFGHIJKLMNOPQRSTUVWXYZ\"\nUpperName <- \"\"\nFOR I <- 1 TO LENGTH(Name)\n    Ch <- SUBSTRING(Name, I, 1)\n    FOR K <- 1 TO 26\n        IF Ch = SUBSTRING(Lower, K, 1) THEN\n            Ch <- SUBSTRING(Upper, K, 1)\n        ENDIF\n    NEXT K\n    UpperName <- UpperName & Ch\nNEXT I\nName <- UpperName\n\nTotal <- 0\nFOR I <- 1 TO 5\n    OUTPUT \"Enter time for run \" & I & \" (seconds):\"\n    INPUT Times[I]\n    Total <- Total + Times[I]\nNEXT I\n\nBest <- Times[1]\nFOR I <- 2 TO 5\n    IF Times[I] < Best THEN\n        Best <- Times[I]\n    ENDIF\nNEXT I\n\nAverage <- Total / 5\nRating <- GetRating(Best)\n\nCALL PrintDivider()\nOUTPUT \"Athlete: \" & Name\nOUTPUT \"Best time: \" & Best & \"s\"\nOUTPUT \"Average:   \" & Average & \"s\"\nOUTPUT \"Rating:    \" & Rating\nCALL PrintDivider()\n\nOPENFILE \"report.txt\" FOR WRITE\nWRITEFILE \"report.txt\", \"Athlete: \" & Name\nWRITEFILE \"report.txt\", \"Best time: \" & Best & \"s\"\nWRITEFILE \"report.txt\", \"Average: \" & Average & \"s\"\nWRITEFILE \"report.txt\", \"Rating: \" & Rating\nCLOSEFILE \"report.txt\"\n\nOUTPUT \"--- Saved Report ---\"\nOPENFILE \"report.txt\" FOR READ\nWHILE NOT EOF(\"report.txt\")\n    READFILE \"report.txt\", Line\n    OUTPUT Line\nENDWHILE\nCLOSEFILE \"report.txt\""
}
];
